import { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "com.taskedo.darsylms",
  appName: "Darsy LMS",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
}

export default config
